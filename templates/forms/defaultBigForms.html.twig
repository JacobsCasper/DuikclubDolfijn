{% extends 'base.html.twig' %}

{% block body %}
    {% if header is defined%}
        <h5 class="card-title text-center">{{ header }}</h5>
    {% endif %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}