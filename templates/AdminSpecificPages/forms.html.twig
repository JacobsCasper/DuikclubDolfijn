{% extends 'base.html.twig' %}

{% block body %}

    {% if forms %}
        <table class="table table-hover">
            <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Titel</th>
                <th scope="col">Antwoorden</th>
                <th scope="col">Edit</th>
                <th scope="col">Delete</th>
            </tr>
            </thead>
            <tbody>
            {% for form in forms %}
                <tr>
                    <td>{{ form.id }}</td>
                    <td>{{ form.title }}</td>
                    <td><a class="btn btn-info" href="{{ path('answers', {'id': form.id}) }}">
                            <i class="fas fa-tasks"></i>&nbsp;&nbsp;
                            Antwoorden
                        </a></td>
                    <td><a class="btn btn-warning" href="{{ path('editForm', {'id': form.id}) }}">
                            <i class="fas fa-edit"></i>&nbsp;&nbsp;
                            Edit
                        </a></td>
                    <td><a class="btn btn-danger" href="#">
                            <i class="fas fa-trash"></i>&nbsp;&nbsp;
                            Delete
                        </a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endif %}
    <div style="display: flex; justify-content: center; align-items: center;">
        {{ knp_pagination_render(forms) }}
    </div>

    <div class="container">
        <div class="row">
            <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
                <div class="card card-signin my-5">
                    <div class="card-body">
                        <h5 class="card-title text-center">Nieuw form aanmaken</h5>
                        {% if is_granted('ROLE_ADMIN') %}
                            {{ form_start(form) }}
                            {{ form_widget(form) }}
                            {{ form_end(form) }}
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}

{% endblock %}